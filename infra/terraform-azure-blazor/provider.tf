provider "azurerm" {
  features {}

  # Optional: Specify the subscription ID if needed
  # subscription_id = "your-subscription-id"

  # Optional: Specify the tenant ID if needed
  # tenant_id = "your-tenant-id"
}